# Public Vite env. Anything prefixed VITE_ is exposed to client code via
# import.meta.env. Validated at boot in src/lib/env.ts via @ghostapi/config.

VITE_API_URL=http://localhost:3001
VITE_APP_NAME=GhostAPI
